PORT=3001
DATABASE_URL=postgresql://mermaids:DEIN_PASSWORT@localhost:5432/mermaids

ALLOWED_ORIGINS=http://178.105.92.40

# Sicheren Zufallsstring generieren:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=AENDERN_VOR_PRODUKTION

# VAPID Keys generieren:
# node -e "const wp=require('web-push'); const k=wp.generateVAPIDKeys(); console.log(k)"
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CONTACT=mailto:admin@mermaids.at

# Optional: SMTP für E-Mails
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@mermaids.at
